<div class="container py-4">
	<div class="row justify-content-center">
		<p class="flash-message text-danger text-center">
    	<%= flash[:add_work_faled] %>
  	</p>
		<%= render 'form', work: @work, staff: @staff %>
		<%= link_to 'Back', staff_path(@staff), class: "btn btn-outline-dark" %>
	</div>
</div>